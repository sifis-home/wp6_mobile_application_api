#!/bin/bash

# Here we could, for example, copy default configuration files over the
# user-defined one

# However, we only print a message to indicate that the script was run
SCRIPT="${0##*/}"
echo "${SCRIPT} was run"
